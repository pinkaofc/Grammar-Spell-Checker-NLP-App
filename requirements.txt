# gradio is installed by Hugging Face from sdk_version in README.md.
# Flask and gunicorn are only needed for flask_app.py / the Dockerfile.
transformers==4.56.1
torch==2.8.0
textblob==0.19.0
nltk==3.9.1
numpy==2.2.6
flask==3.1.2
gunicorn==23.0.0
